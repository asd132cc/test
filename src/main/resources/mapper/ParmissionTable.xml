<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.test.mapper.PermissionTableMapper">

    <select id="selectPermissionByLimit"  resultType="PermissionTable">
        select * from permissionstable where 1=1 and deleted_id =#{deletedId}
        limit ${currPage},${pageSize}
    </select>

    <select id="selectPermission" resultType="PermissionTable" >
        select * from permissionstable where user_name = #{userName} and password= #{password} and deleted_id =#{deletedId}

    </select>


</mapper>